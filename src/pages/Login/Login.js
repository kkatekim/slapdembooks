import React from 'react';
import './Login.js';

const Login = (props) => (
    <div className="Page Login">
        <BackgroundContainer>
            
        </BackgroundContainer>
    </div>
);

export default Login;